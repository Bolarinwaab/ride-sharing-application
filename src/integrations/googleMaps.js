async function route({origin,destination,apiKey,fetchImpl=fetch}){if(!origin||!destination||!apiKey)throw new Error('origin, destination and apiKey are required');const response=await fetchImpl('https://routes.googleapis.com/directions/v2:computeRoutes',{method:'POST',headers:{'Content-Type':'application/json','X-Goog-Api-Key':apiKey,'X-Goog-FieldMask':'routes.duration,routes.distanceMeters,routes.polyline.encodedPolyline'},body:JSON.stringify({origin:{address:origin},destination:{address:destination},travelMode:'DRIVE'})});if(!response.ok)throw new Error(`Google Routes request failed: ${response.status}`);return response.json();}
async function geocode(address,apiKey,fetchImpl=fetch){const url=new URL('https://maps.googleapis.com/maps/api/geocode/json');url.searchParams.set('address',address);url.searchParams.set('key',apiKey);const response=await fetchImpl(url);if(!response.ok)throw new Error(`Google Geocoding request failed: ${response.status}`);const data=await response.json();if(data.status!=='OK'||!data.results?.length)throw new Error(`Geocoding failed: ${data.status}`);return data.results[0];}
module.exports={route,geocode};